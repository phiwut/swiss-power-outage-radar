---
import { APP_LOCALES, HTML_LANG, alternatePath, t, type AppLocale } from "../../i18n";

interface Props {
  locale: AppLocale;
  pathname?: string;
  class?: string;
}

const { locale, pathname = Astro.url.pathname, class: className } = Astro.props;
---

<nav class:list={["app-langs", className]} aria-label={t(locale, "lang.switcher")}>
  {APP_LOCALES.map((code) => (
    <a
      href={alternatePath(pathname, code)}
      data-locale={code}
      lang={HTML_LANG[code]}
      aria-current={code === locale ? "page" : undefined}
    >
      {t(locale, `lang.${code}`)}
    </a>
  ))}
</nav>
