---
import {
  Breadcrumb,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbList,
  BreadcrumbPage,
  BreadcrumbSeparator
} from "@/components/starwind/breadcrumb";
import { t, type AppLocale } from "../../i18n";

interface Crumb {
  href?: string;
  label: string;
}

interface Props {
  items: Crumb[];
  locale?: AppLocale;
}

const { items, locale = "de" } = Astro.props;
---

<Breadcrumb aria-label={t(locale, "nav.breadcrumbAria")} class="page-crumb">
  <BreadcrumbList>
    {items.map((item, index) => (
      <>
        {index > 0 && <BreadcrumbSeparator />}
        <BreadcrumbItem>
          {item.href && index < items.length - 1 ? (
            <BreadcrumbLink href={item.href}>{item.label}</BreadcrumbLink>
          ) : (
            <BreadcrumbPage>{item.label}</BreadcrumbPage>
          )}
        </BreadcrumbItem>
      </>
    ))}
  </BreadcrumbList>
</Breadcrumb>
